
generator client {
  provider = "prisma-client-js"

}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


model Pessoa {
  id String @id @db.Uuid @default(uuid())
  name String
  email String @unique
  sexo String
  idade Int

  @@index([name])
}
